---
import BaseLayout from '../../layouts/BaseLayout.astro';
import GalleryGrid from '../../components/GalleryGrid.astro';
---

<BaseLayout title="Bikini Gallery — Officially Fawnia" description="Bikini and swimwear photos of Fawnia Mondey — fitness model, national-level competitor and pole dance pioneer.">
  <section class="container gallery-page">
    <header class="gallery-head" data-stagger>
      <p class="eyebrow">Gallery</p>
      <h1><span class="ghost-num" aria-hidden="true">01</span> Bikini</h1>
    </header>
    <GalleryGrid section="bikini" label="Bikini" />
    <p class="back"><a href="/gallery">← All galleries</a></p>
  </section>
</BaseLayout>

<style>
  .gallery-page {
    padding-block: var(--space-section);
  }
  .gallery-head h1 {
    display: flex;
    align-items: baseline;
    gap: var(--space-5);
    flex-wrap: wrap;
    margin: 0;
  }
  .gallery-head .ghost-num {
    font-size: clamp(3rem, 7vw, 6rem);
  }
  .back {
    margin-top: var(--space-10);
  }
</style>
